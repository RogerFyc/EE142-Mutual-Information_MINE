[project]
name = "scn-mine"
version = "0.1.0"
description = "Smooth-Clipped Noise-contrastive MINE (SCN-MINE)."
requires-python = ">=3.10"
dependencies = [
    "numpy",
    "matplotlib",
    "torch",
]

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]

